# T-006 E2E Evidence (2026-03-04)

## Scope

- Task: `T-006 ua-v1-internal-security`
- Goal: verify internal auth (`JWT+HMAC`, scope, replay/audience checks), plus gateway/provider/adapter/worker end-to-end chain.
- Environment: local machine (`localhost`, Asia/Shanghai timezone)

## Execution Window

- Start: 2026-03-04 22:04:00 +0800
- End: 2026-03-04 22:08:10 +0800

## Commands and Results

1. Enforce mode conformance

```bash
pnpm test:conformance
```

Result: `PASS`

Evidence highlights:
- `/v0/context/users/:profileRef`
  - valid scope -> `200`
  - missing scope -> `403`
  - missing headers -> `401`
  - audience mismatch -> `401`
- `/v0/events`
  - valid scope -> `200`
  - missing scope -> `403`
  - missing headers -> `401`
  - replay nonce -> `401` (`AUTH_REPLAY`)
- `adapter-wechat -> gateway /v0/ingest` remains available.

2. Worker pipeline smoke (Postgres + Redis)

```bash
DATABASE_URL=postgresql://localhost:5432/uniassist_gateway \
REDIS_URL=redis://localhost:6379 \
pnpm smoke:redis:e2e
```

Result: `PASS`

Evidence highlights:
- baseline outbox pipeline reached consumed state
- injected failed row retried and consumed
- injected `NOGROUP` recovered automatically
- dead-letter replay succeeded
- replay idempotency verified (`updated: 0`)

3. Audit mode behavior check (manual)

Run in `UNIASSIST_INTERNAL_AUTH_MODE=audit`:
- `GET /v0/context/users/profile:audit-user` without internal headers -> `200`
- `POST /v0/events` without internal headers -> `200`
- `POST /wechat/webhook` -> `200`

Result: `PASS`

Evidence highlights:
- gateway logs produced `internal auth denied` with `AUTH_MISSING`
- gateway metrics contained `internalAuth.requests` entries with:
  - `mode=audit`
  - `result=audit_allow`

## Cleanup Confirmation

- DB smoke rows for run session removed (`outbox_events` check = `0`)
- Redis smoke stream keys for run token removed (`scan` count = `0`)

## Risks / Follow-ups

1. `UNIASSIST_INTERNAL_AUTH_REPLAY_BACKEND=redis` currently logs warning and falls back to in-memory nonce store.
2. Staging still needs:
   - `audit -> enforce` rollout evidence
   - `kid` rotation drill evidence
