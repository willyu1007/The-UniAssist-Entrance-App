# Pure V1 Contract Reset — Roadmap

## Goal
- 冻结 pure-`v1` 的 contract baseline，为后续 runtime、studio、connector 和 legacy cleanup 提供唯一语义基础。

## Inputs
- Parent baseline: `dev-docs/active/ua-pure-v1-platform-rewrite/`
- Reused design inputs:
  - `T-019`
  - `T-021`

## Outputs
- pure-`v1` object model
- pure-`v1` DTO and event model
- `agent-first` run and resume contract
- OpenAPI scope and schema planning baseline

## Exit criteria
- downstream implementation tasks no longer need to decide core DTO names, object ownership, or compat removal semantics
