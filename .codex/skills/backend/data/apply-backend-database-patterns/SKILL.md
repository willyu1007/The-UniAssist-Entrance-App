---
name: apply-backend-database-patterns
description: Apply safe and maintainable backend database patterns (transactions, pagination, soft deletes, query optimization, data consistency).
category: backend/data
ssot_path: .ai/skills/backend/data/apply-backend-database-patterns
---

# apply-backend-database-patterns (entry)

Canonical source: `.ai/skills/backend/data/apply-backend-database-patterns/`

Open `.ai/skills/backend/data/apply-backend-database-patterns/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
