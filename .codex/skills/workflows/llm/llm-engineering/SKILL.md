---
name: llm-engineering
description: Entry workflow for LLM engineering tasks (provider integration, calling wrapper, routing profiles, prompt templates, cost/telemetry, credentials/config keys). Routes to one procedure and enforces required verification.
category: workflows/llm
ssot_path: .ai/skills/workflows/llm/llm-engineering
---

# llm-engineering (entry)

Canonical source: `.ai/skills/workflows/llm/llm-engineering/`

Open `.ai/skills/workflows/llm/llm-engineering/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
