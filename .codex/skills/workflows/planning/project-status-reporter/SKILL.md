---
name: project-status-reporter
description: Read-only progress reporter. Produces a structured status snapshot from existing project/task artifacts (project hub + dev-docs) and includes an actionable next command; never modifies repo files.
category: workflows/planning
ssot_path: .ai/skills/workflows/planning/project-status-reporter
---

# project-status-reporter (entry)

Canonical source: `.ai/skills/workflows/planning/project-status-reporter/`

Open `.ai/skills/workflows/planning/project-status-reporter/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
