---
name: plan-maker
description: Create a goal-aligned macro-level roadmap (dev-docs/active/<task>/roadmap.md) by asking clarifying questions when needed; planning only (no code changes); strong signal words: make plan/roadmap/implementation plan.
category: workflows/planning
ssot_path: .ai/skills/workflows/planning/plan-maker
---

# plan-maker (entry)

Canonical source: `.ai/skills/workflows/planning/plan-maker/`

Open `.ai/skills/workflows/planning/plan-maker/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
