---
name: update-dev-docs-for-handoff
description: Update an existing dev-docs task bundle with progress, decisions, pitfalls, and verification evidence to enable clean handoff, context recovery, or archival; triggers: handoff/update task docs/context reset/archive.
category: workflows/dev-docs
ssot_path: .ai/skills/workflows/dev-docs/update-dev-docs-for-handoff
---

# update-dev-docs-for-handoff (entry)

Canonical source: `.ai/skills/workflows/dev-docs/update-dev-docs-for-handoff/`

Open `.ai/skills/workflows/dev-docs/update-dev-docs-for-handoff/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
