---
name: implement-frontend-routing
description: Implement frontend routing with predictable route definitions, lazy loading, nested layouts, and error/404 handling.
category: frontend/routing
ssot_path: .ai/skills/frontend/routing/implement-frontend-routing
---

# implement-frontend-routing (entry)

Canonical source: `.ai/skills/frontend/routing/implement-frontend-routing/`

Open `.ai/skills/frontend/routing/implement-frontend-routing/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
