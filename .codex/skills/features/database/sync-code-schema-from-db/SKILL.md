---
name: sync-code-schema-from-db
description: Mirror schema changes from a real database (SSOT) into repo artifacts (prisma/schema.prisma + db/schema/tables.json) and refresh LLM context (docs/context/db/schema.json).
category: features/database
ssot_path: .ai/skills/features/database/sync-code-schema-from-db
---

# sync-code-schema-from-db (entry)

Canonical source: `.ai/skills/features/database/sync-code-schema-from-db/`

Open `.ai/skills/features/database/sync-code-schema-from-db/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
