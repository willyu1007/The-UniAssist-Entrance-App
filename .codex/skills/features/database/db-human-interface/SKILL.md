---
name: db-human-interface
description: Human-friendly DB structure query + interactive change drafting (Markdown) on top of the normalized DB schema contract (v2). Produces .ai/.tmp/database artifacts and hands off execution to DB SSOT workflows.
category: features/database
ssot_path: .ai/skills/features/database/db-human-interface
---

# db-human-interface (entry)

Canonical source: `.ai/skills/features/database/db-human-interface/`

Open `.ai/skills/features/database/db-human-interface/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
