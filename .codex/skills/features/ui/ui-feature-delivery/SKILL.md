---
name: ui-feature-delivery
description: Implement React UI features using the data-ui contract system and Tailwind B1 (layout-only), including interface/data contract design, evidence capture in .ai/.tmp/ui/<run-id>/, and mandatory governance gate execution.
category: features/ui
ssot_path: .ai/skills/features/ui/ui-feature-delivery
---

# ui-feature-delivery (entry)

Canonical source: `.ai/skills/features/ui/ui-feature-delivery/`

Open `.ai/skills/features/ui/ui-feature-delivery/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
