---
name: ui-system-bootstrap
description: Bootstrap or repair the repo UI SSOT (ui/tokens, ui/contract, ui/styles, docs/context/ui) for data-ui contract + Tailwind B1; run codegen + validation with an approval gate when overwriting.
category: features/ui
ssot_path: .ai/skills/features/ui/ui-system-bootstrap
---

# ui-system-bootstrap (entry)

Canonical source: `.ai/skills/features/ui/ui-system-bootstrap/`

Open `.ai/skills/features/ui/ui-system-bootstrap/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
