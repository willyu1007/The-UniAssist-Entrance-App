---
name: test-mobile-detox
description: Detox mobile E2E automation for React Native: bootstrap, author flows, run/debug, and produce reliable artifacts for CI.
category: testing
ssot_path: .ai/skills/testing/test-mobile-detox
---

# test-mobile-detox (entry)

Canonical source: `.ai/skills/testing/test-mobile-detox/`

Open `.ai/skills/testing/test-mobile-detox/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
