---
name: test-web-cypress
description: Cypress Web UI E2E automation: bootstrap, author specs, run/debug, and triage failures with consistent artifacts and CI hooks.
category: testing
ssot_path: .ai/skills/testing/test-web-cypress
---

# test-web-cypress (entry)

Canonical source: `.ai/skills/testing/test-web-cypress/`

Open `.ai/skills/testing/test-web-cypress/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
