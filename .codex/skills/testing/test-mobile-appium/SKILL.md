---
name: test-mobile-appium
description: Appium mobile automation: capabilities templates, harness bootstrap, execution, and robust environment/diagnostics for CI or device farms.
category: testing
ssot_path: .ai/skills/testing/test-mobile-appium
---

# test-mobile-appium (entry)

Canonical source: `.ai/skills/testing/test-mobile-appium/`

Open `.ai/skills/testing/test-mobile-appium/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
