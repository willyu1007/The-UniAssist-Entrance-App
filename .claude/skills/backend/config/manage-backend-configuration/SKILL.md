---
name: manage-backend-configuration
description: Manage backend configuration safely with typed, validated settings and clear separation between defaults and secrets.
category: backend/config
ssot_path: .ai/skills/backend/config/manage-backend-configuration
---

# manage-backend-configuration (entry)

Canonical source: `.ai/skills/backend/config/manage-backend-configuration/`

Open `.ai/skills/backend/config/manage-backend-configuration/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
