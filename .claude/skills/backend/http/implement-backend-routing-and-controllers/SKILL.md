---
name: implement-backend-routing-and-controllers
description: Implement or refactor backend HTTP routing and controllers using a layered pattern (routes delegate, controllers validate and call services).
category: backend/http
ssot_path: .ai/skills/backend/http/implement-backend-routing-and-controllers
---

# implement-backend-routing-and-controllers (entry)

Canonical source: `.ai/skills/backend/http/implement-backend-routing-and-controllers/`

Open `.ai/skills/backend/http/implement-backend-routing-and-controllers/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
