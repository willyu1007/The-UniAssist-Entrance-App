---
name: smoke-test-authenticated-api-routes
description: Create a minimal smoke-test plan for authenticated API routes (happy path, one negative case, and persistence checks).
category: backend/testing
ssot_path: .ai/skills/backend/testing/smoke-test-authenticated-api-routes
---

# smoke-test-authenticated-api-routes (entry)

Canonical source: `.ai/skills/backend/testing/smoke-test-authenticated-api-routes/`

Open `.ai/skills/backend/testing/smoke-test-authenticated-api-routes/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
