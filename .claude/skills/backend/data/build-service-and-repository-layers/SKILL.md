---
name: build-service-and-repository-layers
description: Build backend service and repository layers to isolate business logic from persistence and improve testability.
category: backend/data
ssot_path: .ai/skills/backend/data/build-service-and-repository-layers
---

# build-service-and-repository-layers (entry)

Canonical source: `.ai/skills/backend/data/build-service-and-repository-layers/`

Open `.ai/skills/backend/data/build-service-and-repository-layers/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
