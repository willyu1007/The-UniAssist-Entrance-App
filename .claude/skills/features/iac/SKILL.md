---
name: iac
description: Enable and operate the IaC feature (ops/iac SSOT + context overview) so infrastructure ownership and boundaries stay explicit.
category: features
ssot_path: .ai/skills/features/iac
---

# iac (entry)

Canonical source: `.ai/skills/features/iac/`

Open `.ai/skills/features/iac/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
