---
name: ci
description: Enable and operate the CI feature (GitHub Actions / GitLab CI) with repeatable templates, artifact conventions, and opt-in delivery enablement.
category: features
ssot_path: .ai/skills/features/ci
---

# ci (entry)

Canonical source: `.ai/skills/features/ci/`

Open `.ai/skills/features/ci/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
