---
name: release
description: Enable and operate the Release feature (release checklists + changelog conventions + ctl-release) for consistent versioning.
category: features
ssot_path: .ai/skills/features/release
---

# release (entry)

Canonical source: `.ai/skills/features/release/`

Open `.ai/skills/features/release/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
