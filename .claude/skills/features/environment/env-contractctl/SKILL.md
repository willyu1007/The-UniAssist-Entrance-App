---
name: env-contractctl
description: Maintain env contract SSOT (env/contract.yaml), validate env/values + env/secrets refs coverage, and generate env/.env.example + docs/context/env/*. Use when adding/renaming/deprecating config keys.
category: features/environment
ssot_path: .ai/skills/features/environment/env-contractctl
---

# env-contractctl (entry)

Canonical source: `.ai/skills/features/environment/env-contractctl/`

Open `.ai/skills/features/environment/env-contractctl/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
