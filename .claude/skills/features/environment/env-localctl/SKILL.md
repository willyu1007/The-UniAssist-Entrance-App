---
name: env-localctl
description: Bootstrap, diagnose (doctor), and reconcile local dev environment from env contract/values/secret refs; generate .env.local and redacted docs/context/env/effective-*. Use when local env is broken or needs syncing.
category: features/environment
ssot_path: .ai/skills/features/environment/env-localctl
---

# env-localctl (entry)

Canonical source: `.ai/skills/features/environment/env-localctl/`

Open `.ai/skills/features/environment/env-localctl/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
