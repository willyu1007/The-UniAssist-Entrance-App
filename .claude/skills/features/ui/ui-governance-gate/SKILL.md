---
name: ui-governance-gate
description: One-command UI governance gate that enforces the data-ui contract + Tailwind B1 and (optionally) orchestrates ESLint/Stylelint/Playwright under the same evidence run directory.
category: features/ui
ssot_path: .ai/skills/features/ui/ui-governance-gate
---

# ui-governance-gate (entry)

Canonical source: `.ai/skills/features/ui/ui-governance-gate/`

Open `.ai/skills/features/ui/ui-governance-gate/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
