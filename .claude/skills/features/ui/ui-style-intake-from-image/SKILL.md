---
name: ui-style-intake-from-image
description: Convert a UI screenshot into a Style Profile and a minimal change proposal (prefer new theme tokens) for the data-ui contract system, with a mandatory approval gate for any token/contract changes.
category: features/ui
ssot_path: .ai/skills/features/ui/ui-style-intake-from-image
---

# ui-style-intake-from-image (entry)

Canonical source: `.ai/skills/features/ui/ui-style-intake-from-image/`

Open `.ai/skills/features/ui/ui-style-intake-from-image/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
