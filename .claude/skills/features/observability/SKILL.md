---
name: observability
description: Enable and operate the Observability feature (metrics/logs/traces contracts) so telemetry expectations are explicit and LLM-readable.
category: features
ssot_path: .ai/skills/features/observability
---

# observability (entry)

Canonical source: `.ai/skills/features/observability/`

Open `.ai/skills/features/observability/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
