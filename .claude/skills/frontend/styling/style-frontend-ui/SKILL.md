---
name: style-frontend-ui
description: Implement and normalize UI styling within an existing design system using theme tokens, reusable style primitives, responsive rules, and accessibility checks. Primary intent: styling execution consistency; can follow frontend-design when a creative direction needs maintainable implementation.
category: frontend/styling
ssot_path: .ai/skills/frontend/styling/style-frontend-ui
---

# style-frontend-ui (entry)

Canonical source: `.ai/skills/frontend/styling/style-frontend-ui/`

Open `.ai/skills/frontend/styling/style-frontend-ui/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
