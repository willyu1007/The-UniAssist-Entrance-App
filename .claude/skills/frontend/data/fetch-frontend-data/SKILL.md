---
name: fetch-frontend-data
description: Fetch and mutate frontend data with a consistent cache strategy (query keys, invalidation, suspense/loading, error handling, optimistic updates).
category: frontend/data
ssot_path: .ai/skills/frontend/data/fetch-frontend-data
---

# fetch-frontend-data (entry)

Canonical source: `.ai/skills/frontend/data/fetch-frontend-data/`

Open `.ai/skills/frontend/data/fetch-frontend-data/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
