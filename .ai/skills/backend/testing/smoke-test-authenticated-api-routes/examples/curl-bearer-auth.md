# Example: curl with bearer token auth (placeholder)

```bash
curl -X POST "http://localhost:3000/api/resource"   -H "Content-Type: application/json"   -H "Authorization: Bearer <REDACTED_TOKEN>"   -d '{ "name": "example" }'
```

Notes:
- Use test credentials and test environments only.
