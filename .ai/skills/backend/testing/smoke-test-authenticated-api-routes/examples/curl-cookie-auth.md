# Example: curl with cookie-based auth (placeholder)

```bash
curl -X GET "http://localhost:3000/api/protected"   -H "Accept: application/json"   -b "session_cookie=<REDACTED_TOKEN>"
```

Notes:
- Replace `session_cookie` with your project’s cookie name.
- Never paste real tokens into documentation.
