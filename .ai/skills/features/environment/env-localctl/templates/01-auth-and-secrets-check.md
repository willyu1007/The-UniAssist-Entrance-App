# Auth & Secrets Check

## Identity state (human action)

- Logged in to cloud/secret backend? (yes/no)
- If not, run the standard login command for your org/provider.

## Secret references

- Confirm secret refs exist in `env/secrets/<env>.ref.yaml`.
- Confirm secret material is available via approved backend (never via chat).
