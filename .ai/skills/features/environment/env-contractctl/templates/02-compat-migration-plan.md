# Compatibility & Migration Plan

## Change classification

- Backward compatible: (yes/no)
- Requires coordinated rollout: (yes/no)
- Requires secret manager changes: (yes/no)

## Migration steps

1. Contract change merged
2. Local dev refresh (`env-localctl`)
3. Cloud plan/apply (`env-cloudctl`)
4. Post-verify checks

## Rename / deprecation policy

- If rename:
  - Old key:
  - New key:
  - Dual-read window:
  - Dual-write window:
  - Removal date:

## Rollback plan

- How to roll back safely without reintroducing leaked secrets.

## Approvals

- Required approver(s):
- Approval timestamp:
