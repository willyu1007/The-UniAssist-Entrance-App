# Staging environment configuration

environment: staging

# Common settings
replicas: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

# Staging-specific settings
debug: false
logLevel: info

