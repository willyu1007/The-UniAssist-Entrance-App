# Example: Component style pattern

## Pattern
- Define styles close to the component.
- Use semantic names (`container`, `header`, `actions`) not visual-only names (`blueBox`).
- Keep style objects small; extract shared styles if reused across features.

## Acceptance criteria
- Styles are consistent across the feature.
- No “magic numbers” for spacing where tokens exist.
