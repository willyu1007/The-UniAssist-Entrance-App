# Template: Request capture (redacted)

- Endpoint: `METHOD /path`
- Environment: local / staging / production
- Auth method: cookie / bearer / api-key
- Headers (redact secrets):
  - `Authorization: Bearer <REDACTED>`
  - `Cookie: session=<REDACTED>`
- Query/params:
- Body (if any):
- Response:
  - status:
  - body:
- Expected:
  - status:
  - permission/role required:
