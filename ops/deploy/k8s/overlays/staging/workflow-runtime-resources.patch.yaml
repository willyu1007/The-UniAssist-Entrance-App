apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-runtime
  namespace: uniassist-staging
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: workflow-runtime
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
