apiVersion: v1
kind: Service
metadata:
  name: workflow-runtime
  namespace: uniassist-staging
  labels:
    app: workflow-runtime
spec:
  selector:
    app: workflow-runtime
  ports:
    - name: http
      port: 8792
      targetPort: 8792
