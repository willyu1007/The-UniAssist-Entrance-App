apiVersion: v1
kind: Service
metadata:
  name: provider-sample
  namespace: uniassist-staging
  labels:
    app: provider-sample
spec:
  selector:
    app: provider-sample
  ports:
    - name: http
      port: 8890
      targetPort: 8890
  type: ClusterIP
